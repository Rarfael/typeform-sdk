# Typeform SDK

Typeform Forms API client, generated from the OpenAPI spec.

Create, read, update, and delete Typeform forms.

Learn more about Typeform Forms API at [typeform.com](https://typeform.com).

This is an unofficial SDK for the Typeform Forms public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

> TypeScript, Python, PHP, Golang, Lua, JavaScript SDKs, a CLI with an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

## Entities, not endpoints

This SDK exposes the API as a small set of **semantic entities** — Form — that you
call directly, instead of assembling URL paths and query strings. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `load`, `create`, `update`, `remove`):

```ts
const client = new TypeformSDK()
const items = await client.Form().list()
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
// The offline mock starts EMPTY — seed it with the records the test needs.
// Shape: { entity: { <entity-name>: { <id>: <record> } } }
const client = TypeformSDK.test({
  entity: {
    form: {
      test01: { id: 'test01' },
    },
  },
})
const forms = await client.Form().list()
// forms is an array of Form entities, populated with mock data
// — call forms[0].data() for the record itself
console.log(forms)
```

### Python

```python
client = TypeformSDK.test()
forms = client.Form().list()
print(forms)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = TypeformSDK::test([
    "entity" => ["form" => ["test01" => ["id" => "test01"]]],
]);
$forms = $client->Form()->list();
```

### Golang

```go
client := sdk.Test()
result, err := client.Form(nil).List(
    nil, nil,
)
```

### Lua

```lua
local client = sdk.test()
local results, err = client:Form():list()
```

### JavaScript

```js
const client = TypeformSDK.test()
const forms = await client.Form().list()
// forms is an array of entities, populated with mock data
// — call forms[0].data() for the record itself
console.log(forms)
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/typeform` | publish pending — [install from git tag](https://github.com/voxgig-sdk/typeform-sdk/releases) |
| Python | `voxgig-sdk-typeform` | publish pending — [install from git tag](https://github.com/voxgig-sdk/typeform-sdk/releases) |
| PHP | `voxgig-sdk/typeform` | publish pending — [install from git tag](https://github.com/voxgig-sdk/typeform-sdk/releases) |
| Golang | `github.com/voxgig-sdk/typeform-sdk/go` | `go get github.com/voxgig-sdk/typeform-sdk/go@latest` |
| Lua | `voxgig-sdk-typeform` | publish pending — [install from git tag](https://github.com/voxgig-sdk/typeform-sdk/releases) |
| JavaScript | `@voxgig-sdk/typeform-js` | publish pending — [install from git tag](https://github.com/voxgig-sdk/typeform-sdk/releases) |
| Go CLI | `github.com/voxgig-sdk/typeform-sdk/go-cli` | `go install github.com/voxgig-sdk/typeform-sdk/go-cli/cmd/typeform@latest` |
| Go MCP server | `github.com/voxgig-sdk/typeform-sdk/go-mcp` | `go get github.com/voxgig-sdk/typeform-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { TypeformSDK } from '@voxgig-sdk/typeform'

const client = new TypeformSDK({
  apikey: process.env.TYPEFORM_APIKEY,
})

// List all forms (returns FormEntity[] — .data() for the record)
const forms = await client.Form().list()
for (const form of forms) {
  console.log(form)
}
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Lua, JavaScript) | `ts/` `py/` `php/` `go/` `lua/` `js/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o typeform-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "typeform": {
      "command": "/abs/path/to/typeform-mcp"
    }
  }
}
```

## Entities

The API exposes one entity:

| Entity | Description | API path |
| --- | --- | --- |
| **Form** | The Form entity (create, list, load, remove, update). | `/forms` |

The operations available across these entities are **load**, **list**, **create**, **update**, **remove** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from typeform_sdk import TypeformSDK

client = TypeformSDK({
    "apikey": os.environ.get("TYPEFORM_APIKEY"),
})

# List all forms (returns a list, raises on error)
forms = client.Form().list()
for form in forms:
    print(form)

# Load a specific form (returns the record, raises on error)
form = client.Form().load({"id": "example_id"})
print(form)
```

### PHP

```php
<?php
require_once 'typeform_sdk.php';

$client = new TypeformSDK([
    "apikey" => getenv("TYPEFORM_APIKEY"),
]);

// List all forms (returns an array; throws on error)
$forms = $client->Form()->list();
print_r($forms);

// Load a specific form (returns the ENTITY; call data_get() for the record; throws on error)
$form = $client->Form()->load(["id" => "example_id"]);
print_r($form);
```

### Golang

```go
import sdk "github.com/voxgig-sdk/typeform-sdk/go"

client := sdk.NewTypeformSDK(map[string]any{
    "apikey": os.Getenv("TYPEFORM_APIKEY"),
})

// List all forms
forms, err := client.Form(nil).List(nil, nil)
if err != nil {
    panic(err)
}
fmt.Println(forms)
```

### Lua

```lua
local sdk = require("typeform_sdk")

local client = sdk.new({
  apikey = os.getenv("TYPEFORM_APIKEY"),
})

-- List all forms
local forms, err = client:Form():list()
print(forms)

-- Load a specific form
local form, err = client:Form():load({ id = "example_id" })
print(form)
```

### JavaScript

```js
const { TypeformSDK } = require('@voxgig-sdk/typeform-js')

const client = new TypeformSDK({
  apikey: process.env.TYPEFORM_APIKEY,
})

// List all forms (returns an array)
const forms = await client.Form().list()
for (const form of forms) {
  console.log(form)
}
```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

**JavaScript:**
```js
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **TestFeature** | In-memory mock transport for testing without a live server |

Pass custom features via the `extend` option at construction time.

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Lua](lua/README.md)
- [JavaScript](js/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

The OpenAPI spec(s) this SDK was generated from are kept in the
[`.sdk/def/`](.sdk/def/) folder.

- Upstream API: [https://api.typeform.com](https://api.typeform.com)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the Typeform Forms API OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
